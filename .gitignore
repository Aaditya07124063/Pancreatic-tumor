# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# OS Files
.DS_Store
.AppleDouble
.LSOverride

# Virtual Environment
.venv/
venv/
ENV/
env/

# Datasets (Local CT scan images)
train/
test/

# Heavy Deep Learning Model Weights (>100MB, exceeds GitHub limits)
*.keras
*.pt
*.pth
*.h5
*.weights
*.ckpt

# Output logs and caches
outputs_logs/*.log
.dataset_cache.pkl
feature_extraction_outputs/**/*.csv
feature_extraction_outputs/**/*.png

# Archive bundles (exceed GitHub's 100MB per-file limit; their contents are tracked already)
*.zip
